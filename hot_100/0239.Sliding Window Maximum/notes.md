# 239. 滑动窗口最大值

> 难度: Hard | 分类: 数组, 前缀和, 哈希表 | 日期: 2026-03-13

## 题目摘要

给你一个整数数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧。
你只可以看到在滑动窗口内的 k 个数字。滑动窗口每次只向右移动一位。返回滑动窗口中的最大值。

## 解题思路

### 第一反应
首先想到的是定长滑动窗口解决，但是由于时间复杂度为O(nk),超过了执行时间，所以需要找到一个查找最大值为O(1)的解法
所以就想到了用单调队列的方式，用一个队列来存储下标，队首始终是当前的最大值，然后每一个元素入列时如果就把比他小的元素排除掉，因为其他元素不可能成为窗口内最大的数了，然后如果队首比left要小，即不在窗口范围内了，也把他pop掉
简单来说就是：队首始终是当前窗口的最大值，然后队尾进入元素不断维护队列的单调性

### 最终解法
1.单调队列套路
    右边入（元素进入队尾，同时维护队列单调性）
    左边出（元素离开队首）
    记录/维护答案（根据队首）

### 复杂度分析
| 解法 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 定长滑动窗口 | O(n^2) | O(n) | 
| 单调队列 | O(n) | O(n) |
## 关键知识点

- [1] 单调队列的查找最大值的时间复杂度是O(1)



## 踩坑记录

- ⚠️ 没有想到用单调队列

## 总结
对于连续子数组的求和问题，可以想到用前缀和来解决
它的核心优势在于：能在 O(1) 的时间内找到当前区间的最大值或最小值，而维护整个区间的总时间复杂度只有 O(n)。
常见的几个适用场景：
①滑动窗口最值问题 (The Classic)
②带有长度限制的最优子段和
优化动态规划 (DP Optimization)