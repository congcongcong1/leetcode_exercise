# 239. 滑动窗口最大值

> 难度: Hard | 分类: 数组, 前缀和, 哈希表 | 日期: 2026-03-13

## 题目摘要

给你一个整数数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧。
你只可以看到在滑动窗口内的 k 个数字。滑动窗口每次只向右移动一位。返回滑动窗口中的最大值。

## 解题思路

### 第一反应
首先想到的是定长滑动窗口解决，但是由于时间复杂度为O(nk),超过了执行时间，所以需要找到一个查找最大值为O(1)的解法
所以就想到了用单调队列的方式，用一个队列来存储下标，队首始终是当前的最大值，然后每一个元素入列时如果就把比他小的元素排除掉，因为其他元素不可能成为窗口内最大的数了，然后如果队首比left要小，即不在窗口范围内了，也把他pop掉
简单来说就是：队首始终是当前窗口的最大值，然后队尾进入元素不断维护队列的单调性

### 最终解法
对于连续子数组的求和问题，可以想到用前缀和来解决：任意子数组都是一个前缀去掉前缀后的结果。所以任意子数组的和，都可以表示为两个前缀和的差。规定前缀和数组，S[0]=0, S[i]=0+^+a[i-1]  S[i+1] = S[i]+a[i]
子数组和为k，即[i,j-1]的子数组和为k，那么s[j]-s[i] = k 那么是不是转化成了两数之和的形式
可以很容易想到用hash表来记录[s[i]:i,S[i+1]:i+1] ，然后查找s[j]-k 是否在hash表中，也就是枚举右寻找左

### 复杂度分析
| 解法 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 前缀和+hash表 | O(n) | O(n) | 
## 关键知识点

- [1] 解决连续子数组求和问题可以维护一个前缀和数组
- [2] 可以用hash表来记录前缀和中每个元素出现的次数


## 踩坑记录

- ⚠️ 没有想到前缀和数组

## 相关题目

| 题号 | 题目 | 关系 |
|------|------|------|
| 15 | 3Sum | 进阶：三数之和 |
| 167 | Two Sum II | 变体：输入有序 |
| 560 | Subarray Sum Equals K | 进阶：前缀和 + 哈希 |

## 总结
对于连续子数组的求和问题，可以想到用前缀和来解决