# 🚀 LeetCode 练习仓库

> 面向实习准备的 LeetCode 刷题仓库，按算法分类组织，配套思考笔记与进度追踪。

## 📁 目录结构

```
├── 01_array/               数组
├── 02_linked_list/         链表
├── 03_stack_queue/         栈与队列
├── 04_hash_table/          哈希表
├── 05_string/              字符串
├── 06_binary_tree/         二叉树
├── 07_backtracking/        回溯
├── 08_greedy/              贪心
├── 09_dynamic_programming/ 动态规划
├── 10_graph/               图论
├── 11_binary_search/       二分查找
├── 12_two_pointers/        双指针 / 滑动窗口
├── 13_sorting/             排序
├── 14_math/                数学
├── 15_design/              设计题
├── templates/              模板文件
└── PROGRESS.md             刷题进度追踪
```

每道题一个文件夹，包含：
- `solution.py` — 解题代码（含多种解法 + 测试用例）
- `notes.md` — 思路分析与复盘笔记

## 🗺️ 推荐刷题路线

| 阶段 | 分类 | 目标题数 |
|------|------|---------|
| **阶段一：基础** | 数组、链表、栈与队列、哈希表 | 每类 8-10 题 |
| **阶段二：核心** | 二叉树、二分查找、双指针、字符串 | 每类 8-10 题 |
| **阶段三：进阶** | 回溯、贪心、动态规划 | 每类 10-15 题 |
| **阶段四：扩展** | 图论、排序、数学、设计题 | 每类 5-8 题 |

## ⏱️ 每日练习节奏

1. **新题 2-3 道** — 按分类顺序推进
2. **复习 1-2 道** — 回顾前几天做错 / 不熟的题
3. **笔记整理** — 每道题写 `notes.md`，重点记录**思路**而非代码

## 🔄 做题流程

```
读题(5min) → 独立思考(15min) → 编码(15min) → 提交验证 → 写笔记(5min)
```

> 💡 15 分钟没思路 → 看提示再尝试 → 仍无法解决 → 看题解**理解后默写**

## 🏃 如何开始

1. 进入对应分类文件夹
2. 复制 `templates/` 中的模板到新题目文件夹
3. 按模板格式完成代码和笔记
4. 在 `PROGRESS.md` 中更新进度

## 🚀 运行代码

```bash
cd 01_array/0001_two_sum
python solution.py
```
1. 笔记：

    1. hash表：
    ①很多涉及到「两个变量」的题目，都可以枚举其中一个变量，把它当成常量看待，从而转化成「一个变量」的问题。
    代码实现时，通常来说「枚举右，寻找左」是更加好写的（如两数之和的hash表写法）
    ②set 是无序不重复的数组，且查找的时间复杂度是O(1),可以利用这个性质

    2. 滑动窗口的使用条件：（同向双指针）
    ①有连续的子数组/子字符串
    ②有单调性可以用while
    ③当题目要求子数组内某种元素的频率或不同元素的个数时，哈希表是唯一的选择。
     2.1 定长滑动窗口
     1. 枚举右指针
     2. left = right - n - 1
     3. if left <0 则继续外层循环添加字符
     4. 达到子字符串长度后判断是否符合条件，符合条件则添加答案
     5. 移除左指针的元素

    3. 相向双指针的使用条件：
    ①目标是寻找“一对”元素（而非连续区间）
    ②数组具有有序性，通常是可以手动sort排序的，可通过剪枝操作优化
    ③具有对称性或边界收缩特性，比如回文串，短板效应等
    ④使用快慢双指针(slow,fast)可以保证数组元素相对顺序的不变

    4. 用二分查找的公式代码：
    # 前提条件：有序 ，或者有单调性的搜索空间，时间复杂度 O(log n)
    # 用[left, right]闭区间来帮助理解   找到大于等于target的第一个下标 时间复杂度O(log n)
    # 关键点：找到循环不变量： 也就是L-1不符合要求，R+1符合要求，因此移动L和R时，L = M +1 R = M -1
    # while的条件：left <= right 最终推出循环时，right =left -1 因此返回的值是left 或者right+1

    # lower_bound 返回最小的满足 nums[i] >= target 的下标 i
    # 如果数组为空，或者所有数都 < target，则返回 len(nums)
    # 要求 nums 是非递减的，即 nums[i] <= nums[i + 1]
    def lower_bound(self, nums: List[int], target: int) -> int:
        left, right = 0, len(nums) - 1  # 闭区间 [left, right]
        while left <= right:  # 区间不为空
            # 循环不变量：
            # nums[left-1] < target
            # nums[right+1] >= target
            mid = (left + right) // 2
            if nums[mid] >= target:
                right = mid - 1  # 范围缩小到 [left, mid-1]
            else:
                left = mid + 1  # 范围缩小到 [mid+1, right]
        # 循环结束后 left = right+1
        # 此时 nums[left-1] < target 而 nums[left] = nums[right+1] >= target
        # 所以 left 就是第一个 >= target 的元素下标
        return left
    # 有序数组中二分查找的四种类型（下面的转换仅适用于数组中都是整数
    1. 第一个大于等于x的下标： lower_bound(x)
    2. 第一个大于x的下标：可以转换为`第一个大于等于 x+1 的下标` : lower_bound(x+1)
    3. 最后一个小于x的下标：可以转换为`第一个大于等于 x 的下标` 的`左边位置`, lower_bound(x)-1;
    4. 最后一个小于等于x的下标：可以转换为`第一个大于等于 x+1 的下标` 的 `左边位置`, lower_bound(x+1)-1;

    5. 连续子数组求和的问题：
    ①如果有负数则不单调，不能用while，可以想到用前缀和来解决
    ②对于连续子数组的求和问题，可以想到用前缀和来解决：任意子数组都是一个前缀去掉前缀后的结果。所以任意子数组的和，都可以表示为两个前缀和的差。规定前缀和数组，S[0]=0, S[i]=0+^+a[i-1]  S[i+1] = S[i]+a[i]
    ③ 子数组和为k，即[i,j-1]的子数组和为k，那么s[j]-s[i] = k 那么就转化成了两数之和的形式

    6. 单调队列套路：适合在“限定的窗口范围”内寻找最值，需要满足1 区间 2 单调性，即A不如B那么B可以被永久丢弃3.最值性，即频繁在窗口内找最大最小值
    ①右边入（元素进入队尾，同时维护队列单调性）
    ②左边出（元素离开队首）
    ③记录/维护答案（根据队首）

    7. 矩阵一次旋转等同于两次翻转
        1. 顺时针旋转90度，等同于先主对角线翻转(即转置)，再左右翻转 row.reverse()
        2. 顺时针旋转180度，等同于先上下翻转，matrix.reverse() ，再左右翻转row.reverse()
        3. 顺时针旋转270度（或者逆时针旋转90度），等同于先左右翻转 row.reverse()，再转置